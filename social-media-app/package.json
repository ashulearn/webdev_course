{
  "name": "social-media-app",
  "version": "1.0.0",
  "description": "```mysql\r create database socialmediadb\r ```\r use the root user only for now\r We will do the npm init and npm install",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mysql2": "^2.2.5",
    "sequelize": "^6.6.2"
  }
}
