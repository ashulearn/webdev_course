{
  "name": "web-sockets",
  "version": "1.0.0",
  "description": "```\r web sockets is protocol build on the http protocol. When we communicate through http protocol \r onl the client can send req to the server , there is no way the server can send any info. to the \r client. So web sockets were introduced .\r ```\r # architecture",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.1.2"
  }
}
